name: Bump plugin version on release

on:
  release:
    types: [published]

permissions:
  contents: write
  pull-requests: write

jobs:
  bump-version:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - name: Extract version from release tag
        id: version
        run: |
          # Strip leading 'v' or 'v.' from tag name
          TAG="${{ github.event.release.tag_name }}"
          VERSION="${TAG#v.}"
          VERSION="${VERSION#v}"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Update plugin.json
        run: |
          jq --arg v "${{ steps.version.outputs.version }}" \
            '.version = $v' .claude-plugin/plugin.json > tmp.json \
            && mv tmp.json .claude-plugin/plugin.json

      - name: Check for changes
        id: diff
        run: |
          if git diff --quiet .claude-plugin/plugin.json; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Create PR to bump version
        if: steps.diff.outputs.changed == 'true'
        run: |
          BRANCH="chore/bump-v${{ steps.version.outputs.version }}"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -b "$BRANCH"
          git add .claude-plugin/plugin.json
          git commit -m "chore: bump plugin version to ${{ steps.version.outputs.version }}"
          git push origin "$BRANCH"
          gh pr create \
            --title "Bump plugin version to ${{ steps.version.outputs.version }}" \
            --body "Automated version bump triggered by release ${{ github.event.release.tag_name }}." \
            --head "$BRANCH" \
            --base main
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
